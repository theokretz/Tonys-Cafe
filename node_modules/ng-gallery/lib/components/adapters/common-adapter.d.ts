import { GalleryConfig } from '../../models/config.model';
/**
 * Common properties for both MainAdapter and ThumbAdapter
 */
export declare class HorizontalCommonAdapter {
    slider: HTMLElement;
    config: GalleryConfig;
    readonly panDirection: number;
    readonly scrollSnapType: string;
    get scrollValue(): number;
    get clientSize(): number;
    constructor(slider: HTMLElement, config: GalleryConfig);
    getPanValue(value: number, e: any, behavior: ScrollBehavior): ScrollToOptions;
}
export declare class VerticalCommonAdapter {
    slider: HTMLElement;
    config: GalleryConfig;
    readonly scrollSnapType: string;
    readonly panDirection: number;
    get scrollValue(): number;
    get clientSize(): number;
    constructor(slider: HTMLElement, config: GalleryConfig);
    getPanValue(value: number, e: any, behavior: ScrollBehavior): ScrollToOptions;
}
